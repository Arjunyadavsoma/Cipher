// Add inside your existing `match /users/{userId}` block in firestore.rules.
// Mirrors whatever access pattern you already use for agentContext /
// agentExecutions under the same user doc.

match /knowledge/{layer} {
  allow read, write: if request.auth != null && request.auth.uid == userId;

  match /entries/{entryId} {
    allow read, write: if request.auth != null && request.auth.uid == userId;
  }
}
